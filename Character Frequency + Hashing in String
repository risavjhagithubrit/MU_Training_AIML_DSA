=> Concept:-

MODULE 3 — Character Frequency + Hashing
 
This is one of the most important String patterns for placements.
The key idea:
Whenever the question asks "how many times", "frequency", "duplicate", "unique", "first occurrence", think about hashing/frequency.

3.1 Frequency Array
For lowercase English characters:
int freq[26] = {0};

for(char ch : s){
   freq[ch - 'a']++;
}
Example:
s = "banana"
Frequency:
a → 3
b → 1
n → 2

3.2 Why ch - 'a'?
ASCII:
'a' → 97
'b' → 98
'c' → 99
Therefore:
'b' - 'a'
= 98 - 97
= 1
So:
a → 0
b → 1
c → 2
...
z → 25

3.3 Important Frequency Problems
Easy
Count frequency of characters
Find most frequent character
Find least frequent character
Find first non-repeating character
Find first repeating character
Check if two strings are anagrams
Medium
Group Anagrams
Sort Characters By Frequency
Longest Palindrome
Find All Anagrams in a String

Key Placement Pattern
Whenever you see:
frequency
duplicate
unique
anagram
occurrence
count
Immediately consider:
Hash Map
     OR
Frequency Array


=> Problem:
#include<bits/stdc++.h>
  using namespace std;

  int main(){
    string s = "banana";
    int freq[26] = {0};

    // calculate the frequency
    for(char ch : s){
        freq[ch-'a']++;
    }
     // Printing the frequency of character that atleast 1
    for(int i=0; i<26; i++){
        if(freq[i] >0){
            cout << (char)(i+'a') << ":" << freq[i] << endl;
        }
    }
     return 0;
  }

-> Time Complexity: O(N)
-> Space Complexity: O(K)

=> Valid Anagram Using Hashing

class Solution {
public:
    bool isAnagram(string s, string t) {
        // Hashing Approach
        // Base case
        if(s.size() != t.size()) return false;
        unordered_map<char, int> mp;
        for(char c : s){
            mp[c]++;
        }
        for(char c : t){
            mp[c]--;

            if(mp[c]<0) return false;
        }
        return true;
       
    }
};

=> Group Anagrams using Sorting:

class Solution {
public:
    vector<vector<string>> groupAnagrams(vector<string>& strs) {
        unordered_map<string, vector<string>> mp;
        for(auto x : strs){
            string word = x;
            sort(word.begin(), word.end());
            mp[word].push_back(x);
        }

        vector<vector<string>> ans;
        for(auto x: mp){
            ans.push_back(x.second);
        }
        return ans;
    }
